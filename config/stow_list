# List of directories (in the current config/ directory) that will be used in the symlink creation during install
# Do not include spaces, comment out directories that should be ignored during install
bash
waybar
river
